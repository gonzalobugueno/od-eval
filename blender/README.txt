Render dataset into ..\synthetic by running the following command inside this folder:
    blender --python render-cvat.py

This will make a dataset in the CVAT for images 1.1 format.

This Blender project makes use of